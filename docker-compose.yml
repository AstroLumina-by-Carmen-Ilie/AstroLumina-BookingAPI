services:
  traefik:
    image: traefik:v3.1
    container_name: booking-api-traefik
    restart: unless-stopped
    volumes:
      - ./traefik.yml:/etc/traefik/traefik.yml:ro
      - ./dynamic:/etc/traefik/dynamic:ro
      - ./certs:/certs
      - ./logs:/var/log/traefik
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    networks:
      - booking-api

  booking-api-blue:
    image: ghcr.io/astrolumina-by-carmen-ilie/astrolumina-bookingapi:2.0.1
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3033
    healthcheck:
      test: [ "CMD", "wget", "--spider", "-q", "http://localhost:3033/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.125'
          memory: 128M
    networks:
      - booking-api

  booking-api-green:
    image: ghcr.io/astrolumina-by-carmen-ilie/astrolumina-bookingapi:2.0.1
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3033
    healthcheck:
      test: [ "CMD", "wget", "--spider", "-q", "http://localhost:3033/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.125'
          memory: 128M
    networks:
      - booking-api

networks:
  booking-api:
    name: booking-api-network
    driver: bridge
